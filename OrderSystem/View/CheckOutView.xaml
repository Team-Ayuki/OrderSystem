<UserControl x:Class="OrderSystem.View.CheckOutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderSystem.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock Text="注文内容" FontSize="20" FontWeight="Bold" Margin="10"/>
            <DataGrid Height="390" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="30,0,30,0"/>

        </StackPanel>
        <StackPanel Grid.Column="1">
            <Image Height="130"/>
            <StackPanel Background="Beige">
                <TextBlock Text="合計金額" FontSize="20" FontWeight="Bold" Margin="10"/>
                <TextBlock Text="¥0" FontSize="20" FontWeight="Bold" Margin="10"/>
            </StackPanel>

            <Button Content="注文を確定する" Height="100" Margin="30,30,30,0"/>
            <Button Content="戻る" Height="50" Margin="30,30,30,0"/>
        </StackPanel>

    </Grid>
</UserControl>
